<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fontFamily">sans-serif</item>
    </style>

    <style name="ShapeAppearanceOverlay.RoundedCorner" parent="">
        <item name="cornerSize">10%</item>
    </style>

    <style name="ElevatedView">
        <item name="android:background">@drawable/bg_rounded_corner_unelevated</item>
        <item name="elevation">@dimen/very_small</item>
    </style>

    <style name="RestaurantItemLabel">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">@dimen/medium</item>
        <item name="android:layout_marginLeft">@dimen/medium</item>
    </style>

</resources>
